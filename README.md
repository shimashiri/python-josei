# python-josei
# 🧠 Task Manager API

A production-ready task management backend built with FastAPI.

## Features
- User registration & login
- JWT authentication
- Task CRUD (Create, Read, Update, Delete)
- SQLite database
- Clean project structure

## Run
```bash
pip install -r requirements.txt
uvicorn main:app --reload
